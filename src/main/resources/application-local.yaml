app:
  domain: localhost
  oauth2:
    authorized-redirect-uri: http://localhost:3000/
logging:
  level:
    co.invest72: DEBUG
server:
  servlet:
    session:
      cookie:
        name: INVEST72_SESSION
        http-only: true
        secure: true
        same-site: lax
        domain: localhost
