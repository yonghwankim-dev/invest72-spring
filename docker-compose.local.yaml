services:
  app:
    ports:
      - "8080:8080"
    env_file:
      - .env.local
    extends:
      file: docker-compose.yaml
      service: app
  db:
    env_file:
      - .env.local
    extends:
      file: docker-compose.yaml
      service: db
volumes:
  db-data:
